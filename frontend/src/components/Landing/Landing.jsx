import React from "react";

const Landing = () => {
  return (
    <div>
      <button>Veranstaltungen in deiner Nähe</button>
      <button>Veranstaltungen erstellen</button>
    </div>
  );
};

export default Landing;
